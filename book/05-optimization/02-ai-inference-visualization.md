# 5.2 AI 推理可视化技术

## 开始

在 AI 生成代码的质量管理中,最重要的是明确掌握 AI "推测"了哪些部分。通过使用信号灯系统的 AI 推理可视化技术,可以实现高效的审查和高质量保证。

## 信号灯系统的理论背景

### 课题的特定

AI 生成代码具有以下特性:

- **自动补全的广泛性**: AI 会自动补全未明确指定的部分
- **看似合理的陷阱**: 生成内容看起来合理,但实际与意图不同
- **推测依据不明确**: 不清楚基于什么信息生成

### 解决方法

信号灯系统按以下标准分类 AI 生成内容,明确审查的优先级:

```
🟢 绿灯 → 🟡 黄灯 → 🔴 红灯
  安全      注意       危险
```

## 信号灯系统的详细定义

### 🟢 绿灯(高置信度·安全)

**定义:** 可从参考的原始文件中明确推测的内容

**特征:**
- 基于原始指示或规格说明中明确记载内容的生成
- 遵循现有代码模式的实现
- 有明确依据的实现判断

**具体例子:**
```javascript
// 规格说明中明确标注"用户 ID 是必需的"的情况
function validateUser(userId) {
  if (!userId) {  // 🟢 从规格说明中明确推导
    throw new Error('User ID is required');
  }
}
```

**审查优先级:** 低
**确认要点:** 实现的准确性、性能影响

### 🟡 黄灯(中置信度·注意)

**定义:** 参考的原始文件中没有但认为合理的内容

**特征:**
- AI 基于合理推测的补全
- 基于一般最佳实践的实现
- 利用领域知识的推理

**具体例子:**
```javascript
// 规格说明中没有详细说明的错误处理
function processData(data) {
  try {
    return transform(data);
  } catch (error) {  // 🟡 一般性的但需要确认
    console.error('Data processing failed:', error);
    return null;
  }
}
```

**审查优先级:** 高
**确认要点:** 推测的妥当性、与业务需求的一致性

### 🔴 红灯(需判断·危险)

**定义:** 参考的原始文件中没有且无法直接推测的内容

**特征:**
- AI 基于独立判断的生成
- 假定组织特定的惯例或规则
- 没有明确依据的实现选择

**具体例子:**
```javascript
// 没有关于组织日志格式信息的情况
function logUserAction(action) {
  // 🔴 日志格式是组织特定的,需要确认
  logger.info(`[AUDIT] User performed: ${action} at ${new Date().toISOString()}`);
}
```

**审查优先级:** 最高
**确认要点:** 与组织规则的一致性、安全影响

## 实施方法和 TODO 文件格式

### 标准 TODO 文件格式

```markdown
## [步骤名称]结果 TODO

### 🟢 高置信度项目
- [ ] [utils.js](./src/utils.js) 的类型定义与规格说明一致性确认
- [ ] [validation.js](./src/validation.js) 的必需项目检查实现的确认

### 🟡 中置信度项目
- [ ] [error-handler.js](./src/error-handler.js) 的错误响应格式妥当性确认
- [ ] [config.js](./src/config.js) 的默认值设置的组织政策符合性

### 🔴 需判断项目
- [ ] 详细确认: [logger.js](./src/logger.js) 的日志输出格式符合组织标准
- [ ] 详细确认: [auth.js](./src/auth.js) 的会话管理方式的选择依据
```

### 提示词中的指示方法

**基本指示模板:**
```markdown
## AI 推理可视化指示

请执行以下作业并用信号灯系统分类生成内容:

**作业内容:**
[具体任务内容]

**分类标准:**
- 🟢 绿灯: 可从参考文件([文件名])中明确推导
- 🟡 黄灯: 合理推测但参考文件中未明确标注
- 🔴 红灯: 基于独立判断的生成(组织特定内容等)

**输出文件:** `./todos/[步骤名称]-inference-check.md`

**输出格式:**
为每个生成项目添加信号灯标记,以 TODO 格式创建检查项目
```

### 参考源文件的管理方法

**文件关系性的记录:**
```markdown
## 参考文件管理

**主要参考(Primary References):**
- [`requirements.md`](./docs/requirements.md) - 基本需求定义
- [`api-spec.yaml`](./docs/api-spec.yaml) - API 规格

**辅助参考(Secondary References):**
- [`existing-code/`](./src/existing/) - 现有实现模式
- [`config-samples/`](./config/) - 配置文件示例

**外部参考(External References):**
- 技术文档(框架官方)
- 行业标准(RFC、W3C 等)

**推理依据追踪:**
- 🟢项目 → 主要参考中明确标注
- 🟡项目 → 辅助参考+一般知识
- 🔴项目 → 依据不明·独立判断
```

## 检查优先级的设定

### 优先级矩阵

| 信号 | 影响度·高 | 影响度·中 | 影响度·低 |
|------|-----------|-----------|-----------|
| 🔴 红灯 | **最优先** | 高优先 | 中优先 |
| 🟡 黄灯 | 高优先 | 中优先 | 低优先 |
| 🟢 绿灯 | 中优先 | 低优先 | **延后** |

### 影响度评估标准

**影响度·高:**
- 涉及安全的实现
- 影响数据一致性
- 影响整个系统的运行

**影响度·中:**
- 影响特定功能的运行
- 影响用户体验
- 影响性能

**影响度·低:**
- 日志输出或注释
- 内部变量名
- 辅助功能

### 实践检查顺序

1. **🔴×影响度·高** - 立即确认·修正
2. **🔴×影响度·中** 以及 **🟡×影响度·高** - 下次作业开始前确认
3. **其他🔴项目** - 实现完成前必须确认
4. **🟡项目** - 审查时确认
5. **🟢项目** - 最终检查时确认

## 实际运用例和案例研究

### 案例研究1: REST API 实现

**场景:** 用户管理 API 的实现
**参考文件:** `user-api-spec.yaml`, `existing-user-model.js`

**AI 生成结果的分类:**

```javascript
// 🟢 API 规格说明中明确标注的端点
app.post('/api/users', async (req, res) => {

  // 🟡 一般性的验证但规格说明中没有详细说明
  if (!req.body.email || !req.body.password) {
    return res.status(400).json({ error: 'Email and password required' });
  }

  // 🔴 哈希算法依赖组织特定政策
  const hashedPassword = bcrypt.hashSync(req.body.password, 12);

  // 🟢 遵循现有模型模式的实现
  const user = new User({
    email: req.body.email,
    password: hashedPassword
  });
});
```

**生成的 TODO:**
```markdown
## API 实现结果 TODO

### 🟢 高置信度项目
- [ ] [user-controller.js](./src/controllers/user.js) 的端点定义与规格说明一致
- [ ] [user-model.js](./src/models/user.js) 的现有模式遵循确认

### 🟡 中置信度项目
- [ ] [validation.js](./src/middleware/validation.js) 的错误消息格式妥当性
- [ ] [user-controller.js](./src/controllers/user.js) 的状态码选择确认

### 🔴 需判断项目
- [ ] 详细确认: [auth.js](./src/utils/auth.js) 的 bcrypt 盐轮次数符合组织政策
```

### 案例研究2: 测试用例生成

**场景:** 上述 API 的测试用例创建
**参考文件:** `user-api-spec.yaml`, `existing-test-patterns.js`

**分类结果:**
```javascript
describe('User API', () => {
  // 🟢 规格说明中明确标注的测试用例
  it('should create user with valid email and password', async () => {
    const response = await request(app)
      .post('/api/users')
      .send({ email: 'test@example.com', password: 'password123' });

    expect(response.status).toBe(201);  // 🟢 按规格说明
  });

  // 🟡 一般性的边缘情况(规格说明中未明确标注)
  it('should reject invalid email format', async () => {
    const response = await request(app)
      .post('/api/users')
      .send({ email: 'invalid-email', password: 'password123' });

    expect(response.status).toBe(400);  // 🟡 基于推测
  });

  // 🔴 基于组织特定安全要求的推测
  it('should enforce password complexity requirements', async () => {
    const response = await request(app)
      .post('/api/users')
      .send({ email: 'test@example.com', password: '123' });

    expect(response.status).toBe(400);  // 🔴 依赖组织政策
  });
});
```

## 效果测定和改进方法

### 效果测定指标

**定量指标:**
- 审查时间的缩短率
- 错误发现率的提升
- 修正次数的减少

**定性指标:**
- 审查效率性提升
- 防止重要问题遗漏
- 开发者安心感提升

### 运用数据示例

```markdown
## 信号灯系统导入效果(1个月)

**传统审查:**
- 平均审查时间: 45分钟/功能
- 错误发现率: 约 60%
- 审查遗漏: 月 3-4 件

**信号灯系统导入后:**
- 平均审查时间: 25分钟/功能(缩短 44%)
- 错误发现率: 约 85%(提升 25%)
- 审查遗漏: 月 1 件以下

**🔴项目的典型问题:**
- 安全相关: 40%
- 组织政策违反: 35%
- 配置·环境依赖: 25%
```

### 持续改进的方法

**1. 分类精度的提升:**
```markdown
## 分类标准的改进日志

**Week 1-2:**
- 问题: 日志格式的分类模糊
- 改进: 创建组织特定项目检查清单

**Week 3-4:**
- 问题: 错误处理的分类不稳定
- 改进: 明文化错误处理模式

**Month 2:**
- 问题: 新技术栈的分类困难
- 改进: 创建技术栈别指南
```

**2. 提示词优化:**
```markdown
## 提示词改进循环

**改进前的课题:**
- 🔴项目的检出率约 70%
- 分类缺乏一致性

**改进内容:**
- 提供组织特定项目的明确清单
- 丰富添加判断标准的具体例子

**改进后的效果:**
- 🔴项目检出率提升到 90% 以上
- 分类一致性大幅改善
```

## 实践导入步骤

### Step 1: 基本系统的构建

```bash
# 准备项目结构
mkdir -p todos
mkdir -p docs/inference-guides

# 创建基本模板
cat > docs/inference-guides/classification-template.md << 'EOF'
## AI 推理分类模板

### 🟢 绿灯的判定标准
- 参考文件"[文件名]"中明确标注的内容
- 遵循现有代码已确立模式的实现

### 🟡 黄灯的判定标准
- 基于一般最佳实践的实现
- 技术上合理但参考文件中未明确标注

### 🔴 红灯的判定标准
- 依赖组织特定政策或惯例
- 没有明确依据的独立判断
EOF
```

### Step 2: 组织特定规则的明文化

```markdown
## 组织特定检查点

**安全相关:**
- [ ] 密码哈希算法和强度
- [ ] 会话管理方式
- [ ] API 认证方式

**日志·审计相关:**
- [ ] 日志输出格式和级别
- [ ] 审计日志的输出项目
- [ ] 日志保存期限和轮换

**编码规范:**
- [ ] 命名规则(变量、函数、类)
- [ ] 错误处理模式
- [ ] 注释描述规则
```

### Step 3: 团队运用的确立

```markdown
## 团队运用规则

**分类作业的负责:**
- AI 执行者进行初始分类
- 审查者确认分类的妥当性

**检查作业的分担:**
- 🔴项目: 高级工程师确认
- 🟡项目: 团队内配对审查
- 🟢项目: 自动测试+轻微确认

**知识积累:**
- 每周一次的分类标准检讨会议
- 误分类模式的共享
- 改进案例的文档化
```

## 故障排除

### 常见问题和对策

**问题1: 分类不一致**

```markdown
**症状:** 相同内容但分类结果不同
**原因:** 分类标准模糊、提示词指示不明确
**对策:**
1. 创建更具体的组织特定项目清单
2. 在提示词中包含过去的分类示例
3. 记录判断困难事项并标准化
```

**问题2: 🔴项目的遗漏**

```markdown
**症状:** 重要的组织特定项目被分类为🟡或🟢
**原因:** AI 不了解组织的上下文
**对策:**
1. 提供组织特定项目的明确清单
2. 设定"可疑情况分类为🔴"的原则
3. 审查者进行分类妥当性检查
```

**问题3: TODO 项目过多**

```markdown
**症状:** 生成的 TODO 项目数超出可执行范围
**原因:** 分类过细、影响度评估过宽
**对策:**
1. 类似项目的分组
2. 严格化影响度评估
3. 导入"重要度×紧急度"矩阵
```

## 实践练习

### 练习1: 分类标准的创建

请用信号灯系统分类以下代码片段:

```javascript
function authenticateUser(username, password) {
  // 案例1: 用户存在检查
  const user = await User.findOne({ username });
  if (!user) {
    return { success: false, message: 'User not found' };
  }

  // 案例2: 密码验证
  const isValid = await bcrypt.compare(password, user.hashedPassword);
  if (!isValid) {
    return { success: false, message: 'Invalid password' };
  }

  // 案例3: JWT 令牌生成
  const token = jwt.sign(
    { userId: user.id, role: user.role },
    process.env.JWT_SECRET,
    { expiresIn: '24h' }
  );

  // 案例4: 日志输出
  console.log(`User ${username} authenticated successfully at ${new Date().toISOString()}`);

  return { success: true, token };
}
```

**参考文件:** `auth-spec.md`(仅记载基本认证流程)

### 练习2: TODO 项目的优先级设定

基于上述分类结果,请按优先级顺序排列 TODO 项目。

## 总结

通过 AI 推理可视化技术可以实现以下效果:

1. **高效审查**: 集中于重要部分的质量检查
2. **降低风险**: 确实发现高风险推测部分
3. **知识积累**: 明文化和共享组织特定判断标准
4. **持续改进**: 基于数据的分类标准和提示词优化

信号灯系统是在 AITDD 中平衡人为因素和 AI 支持的重要技术。下一节将学习利用这些技术的持续改进和提示词优化。
